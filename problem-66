#include <iostream>
#include <math.h>
using namespace std;

int main() {
	int n;
	cout<<"Enter a decimal number:";
	cin>>n;
	int binary = 0;
	int r, i = 1;
	
	 while (n!=0)
	 {
	 	r = n%2;
	 	n = n/2;
	 	binary = binary+(r*i);
	 	i = i*10;
	 }
	 cout<<"the binary number is:"<<binary;
	return 0;
}
